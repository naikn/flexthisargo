 apiVersion: v1
 kind: Pod
 metadata:
   name: hook-test
   annotations:
     "argocd.argoproj.io/hook": "PostSync"
 spec:
   containers:
   - name: hook2-container
     image: busybox
     imagePullPolicy: IfNotPresent
     command: ['sh', '-c', 'echo post-install hook Pod is running - post sync fail && exit 1']
   restartPolicy: Never
   terminationGracePeriodSeconds: 0  
